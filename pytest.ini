[pytest]
testpaths = packages/agent/src/tests
pythonpath = packages/agent/src packages/preprocessor/src
python_files = test_*.py
addopts = -ra

# archive/ contains historical experiments and is never part of the default
# release suite. Run those files explicitly when investigating legacy behavior.
norecursedirs = .git .venv node_modules archive .pytest_cache
