# 沟通日志 — 2026-06-05

> 项目：网络安全移动运营商智能 Agent

---

## 今日目标

1. ✅ 重启服务验证 `agent.py:721` 自检 timeout 修改（15→300）生效
2. ✅ 修复 LLM-as-Judge 评分管线（str.format() 转义 Bug + 统一 _llm_judge 助手）
3. ✅ 测试集重新平衡 + 去除运营商标记
4. ✅ 结项归档所有改动

---

## 状态确认

### 代码已改好（6月4日遗留）

`packages/agent/src/agent.py` line 721 自检 LLM 调用的 timeout **已改为 300**：

```python
result = self.llm.chat(
    [{"role": "user", "content": prompt}],
    temperature=0.0,
    max_tokens=2048,
    timeout=300,  # 昨天已从 15 改为 300
)
```

**待做**：重启 `python main.py` 后验证硅基流动评测不再超时。

---

## 执行记录

| 时间 | 事项 | 结果 |
|------|------|------|
| — | — | — |
| 上午 | promptEval 卡走硅基流动 DeepSeek-V4-Flash，LLM-as-Judge 全部回退启发式 | 定位到 `str.format()` 未转义 `{}` |
| 下午 | 修复 5 个评分 prompt 中的 `{}` 转义问题 | 5/5 LLM 评分正常，不再回退 |
| 下午 | 加 `_clean_answer()` 清洗引用标记 | 不影响前台，评分更准 |
| 下午 | 去除 8 个文件中所有"运营商"关键词 | 知识库方向面向通用网络安全 |
| 下午 | 测试集重新平衡 | 等保 10→5，新增应急响应/风险评估/灾难恢复/业务连续性 |
| 下午 | 结项归档 | 更新改善计划、沟通记录 |

---

## 详细改动记录

### 1. 评测管线修复

**根因**：Python `str.format()` 中未转义的 `{}` 导致 KeyError。5 个评分 prompt（`_eval_generation.py` 3 个 + `_eval_context.py` 2 个）中的 JSON 示例 `{"score": ...}` 被 `format()` 误解析为占位符。

**修复**：
- 所有 prompt 中 JSON 示例的 `{` → `{{`，保留 `{query}` `{context}` `{answer}` 占位符不变
- 统一 `_llm_judge()` 助手函数：`temperature=0.01` + `_extract_json()` + 统一异常处理
- 加 `_clean_answer()`：评分前移除 `【思考过程】` 和 `[来源N: ...]` 元数据

**涉及文件**：`_eval_generation.py`, `_eval_context.py`, `main.py`

### 2. 去除"运营商"特指

**改动**：8 个文件中所有涉及"移动运营商""运营商"的关键词清理
- `agent.py`：System Prompt、招呼回复共 10 处
- `main.py`：评测 LLM prompt、Word 导出 footer、测试题生成 prompt 共 6 处
- `eval_e2e.py` / `eval_30_v3.py` / `prompt_test_manager.py`：测试题表述
- `templates/admin.html` / `index.html` / `static/prompt_redesign_preview.html`：前台文案

### 3. 测试集重新平衡

| 领域 | eval_e2e.py | eval_30_v3.py |
|:----|:-----------:|:-------------:|
| 等保合规 | 10→**5** | 10→**5** |
| 数据安全 | 5→**5** | — |
| 安全运营 | 5→**4** | — |
| 管理体系 | 5→**4** | — |
| 基础设施/CII | 5→**4** | 6→**4** |
| 国家法律 | — | 8→**5** |
| 通信行业 | — | 3→**3** |
| 跨领域综合 | — | 3→**3** |
| **应急响应** | **+3** | **+3** |
| **风险评估** | **+3** | **+3** |
| **灾难恢复** | **+1** | **+2** |
| **业务连续性** | **+1** | **+2** |
| **总计** | **30** | **30** |

### 4. 剩余问题

- 硅基流动 API 响应慢，`read timeout=10` 偶尔超时。promptEval 卡已自动同步为 chat 卡的同款模型配置，可手动切到 DeepSeek 官方 API 提升稳定性

---

## 下午/晚间追加（可维护性优化 + 安全修复）

| 时间 | 事项 | 结果 |
|------|------|------|
| 下午 | main.py 拆出 auth.py（113行）+ llm_config_manager.py（251行） | main.py 从 3650+ 减到 ~3552 行 |
| 下午 | 创建 _eval_common.py 公共模块（compute_avg_stats/score_single_question/render_html_page） | 消除 eval_e2e.py 和 eval 脚本的重复代码 |
| 下午 | 创建 tests/test_core.py，32 个测试 | 全部通过，覆盖 avg_stats/extract_json/clean_answer/CircuitBreaker/validate_llm_url/is_admin_route/verify_admin_token |
| 下午 | 路由认证保护（CRITICAL-2） | /api/documents/* 和 /api/llm/configs/* 移入受保护路由 |
| 下午 | SQLite WAL 模式（MAJOR-3） | memory.py 写入不阻塞读取 |
| 下午 | 清理临时文件 | 删除了 _direct_api_test.py 等 7 个 + 根目录 11 个零散文件 |
| 下午 | 复制项目到桌面 | 路径: D:\OneDriver\OneDrive\Desktop\网络安全-移动运营商智能Agent（不含本地数据和临时文件）|
| 晚间 | 修复 admin_model_preview.html 和 data_preview.html 缺少 Token 注入 | 两个管理页面加了 window.fetch 覆写 + 自动注入 X-Admin-Token |
| 晚间 | 修复 timeout=10 硬编码 | agent.py query 改写和 memory.py 摘要 timeout 10→30 |
| 晚间 | /admin/documents 和 /admin/model-config 加 Cache-Control: no-cache | 解决浏览器缓存旧 HTML 导致的 403 |

### 待排查问题

- **增量索引失败**：上传文档后 `❌ 增量索引失败`，Ollama 运行正常（quentinz/bge-small-zh-v1.5 已加载），可能是个别 .md 文件格式异常导致子进程报错。需手动跑 `python incremental_index.py` 看完整 stderr。

---

## 晚间追加 ② — 审计回复 + 代码质量审计

| 时间 | 事项 | 结果 |
|------|------|------|
| 22:41 | 全量重建 FAISS + Chroma（11103 chunks） | 修复增量索引脏数据积累 |
| 23:00 | git commit `17a9b88` | 21 文件 +1060/-3330 |
| 23:20 | 修复 `_verify_answer` PASS 剪裁 + CDN integrity | `agent.py:730` / `admin.html:7-8` |
| 23:30 | 修复 `ask_stream` 跳过自检 + `build_parent_index` 空节 + eval 端口残留 | `agent.py:1449` / `build_parent_index.py:54-56` / `eval_30_v3.py:407/450/482` |
| 23:40 | 两份审计报告写入第一轮 + 第二轮回复 | 全面安全审计报告.docx + Code_Audit_Report_Combined.docx |

### 审计方反馈（复核后确认）

审计方 23:48 审核了桌面副本（旧快照），发现"7/8 修复不存在"。后重新审核原始仓库（commit `17a9b88`），**确认全部 8 项修复存在**。根因：桌面副本是 21:00 生成的快照，不包含后续修复。

### 第9章 — 代码质量审计问题记录（待明日修复）

审计方于 6 月 6 日 00:00 新增第 9 章代码质量评估，总分 **17/30**。问题清单：

#### Critical / High

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| H-1 | 裸 `except:`（7 处） | `llm_config_manager.py:102`, `main.py:77/121/1973`, `memory.py:1037/1159`, `prompt_tester.py:466` | 会捕捉 KeyboardInterrupt/SystemExit，Ctrl+C 无法终止进程 |
| H-2 | 273 个 `print()` 调用 | `_eval_context.py`, `_eval_generation.py`, `_verify_faithfulness.py`, `index_batch.py`, `incremental_index.py` | 生产 stdout 污染，无日志级别管控 |
| H-3 | `main.py` 3552 行 | 单体文件 | 建议拆为 routes_admin.py / routes_api.py / websocket_handler.py / app_lifespan.py |

#### Medium

| # | 问题 |
|---|------|
| M-1 | `memory.py` 1472 行，建议拆缓存/持久化/查询三模块 |
| M-2 | 160 函数（41%）缺 docstring |
| M-3 | 440 行超 100 字符限制（PEP 8） |
| M-4 | `__init__.py` 空文件，缺 `__all__` |
| M-5 | 加密密钥自动生成无提示，丢失后所有 API Key 不可解密 |

#### Low

| # | 问题 |
|---|------|
| L-1 | 中英文日志混用 |
| L-2 | 硬编码阈值散落多处（offtopic/greeting/MAX_CONTEXT_TOKENS 等） |
| L-3 | 冗余 import |
| L-4 | 无 formatter/linter 配置（.pylintrc / .flake8 / pyproject.toml） |

**计划**：明日优先修复 H-1（bare except）和 H-2（print→logger），然后处理 M 级问题。